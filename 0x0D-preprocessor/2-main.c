#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * Description: prints the name of the file it was compiled from
 * Return: Always 0 (success)
 */

int main(void)
{
	puts(__FILE__);
	return (0);
}
